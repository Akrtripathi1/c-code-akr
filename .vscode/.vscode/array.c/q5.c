#include<stdio.h>
int duplicate(int b[],int n)
{
        int i,j;
        for(i=0,j=1;i<=n-1,j<=n-1;i++,j++)
          {
              if(b[i]==b[j])
        
                return b[i];
              
          }
}